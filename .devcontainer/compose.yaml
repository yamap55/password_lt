version: "3"
services:
  app:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
      args:
        WORKDIR: /project
    volumes:
      - ..:/project
    image: password_lt-image
    container_name: password_lt-container
    tty: true
    env_file: ../.env # shellスクリプト内でも環境変数として使用するため
